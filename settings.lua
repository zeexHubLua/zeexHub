-- settings.lua
local Settings = {
    Colors = {
        mainBg = Color3.fromRGB(0, 20, 40),
        panelBg = Color3.fromRGB(0, 40, 70),
        button = Color3.fromRGB(0, 100, 200),
        buttonAlt = Color3.fromRGB(0, 150, 255),
        text = Color3.fromRGB(200, 230, 255),
        accent = Color3.fromRGB(0, 200, 255)
    },
    
    Macro = {
        savePath = "ZeexLUA_macros.txt",
        autoSave = true,
        checkInterval = 0.2
    }
}

return Settings-- settings.lua
local Settings = {
    Colors = {
        mainBg = Color3.fromRGB(0, 20, 40),
        panelBg = Color3.fromRGB(0, 40, 70),
        button = Color3.fromRGB(0, 100, 200),
        buttonAlt = Color3.fromRGB(0, 150, 255),
        text = Color3.fromRGB(200, 230, 255),
        accent = Color3.fromRGB(0, 200, 255)
    },
    
    Macro = {
        savePath = "ZeexLUA_macros.txt",
        autoSave = true,
        checkInterval = 0.2
    }
}

return Settings
